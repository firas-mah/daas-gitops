apiVersion: apps/v1
kind: Deployment
metadata:
  name: monotest2-back
  namespace: monotest2
  labels:
    app: monotest2-back
    app.kubernetes.io/part-of: monotest2
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monotest2-back
      app.kubernetes.io/part-of: monotest2
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app: monotest2-back
        app.kubernetes.io/part-of: monotest2
        app.kubernetes.io/component: back
    spec:
      containers:
        - name: back
          image: docker.io/firasmah/monotest2-back:run-1774735962039-back
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: monotest2-back-secrets
                optional: true
            - configMapRef:
                name: monotest2-back-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
