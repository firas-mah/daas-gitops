apiVersion: apps/v1
kind: Deployment
metadata:
  name: monotest2-front
  namespace: monotest2
  labels:
    app: monotest2-front
    app.kubernetes.io/part-of: monotest2
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monotest2-front
      app.kubernetes.io/part-of: monotest2
      app.kubernetes.io/component: front
  template:
    metadata:
      labels:
        app: monotest2-front
        app.kubernetes.io/part-of: monotest2
        app.kubernetes.io/component: front
    spec:
      containers:
        - name: front
          image: docker.io/firasmah/monotest2-front:run-1774735962141-front
          ports:
            - containerPort: 80
              protocol: TCP
          envFrom:
            - secretRef:
                name: monotest2-front-secrets
                optional: true
            - configMapRef:
                name: monotest2-front-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
