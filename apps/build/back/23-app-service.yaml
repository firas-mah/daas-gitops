apiVersion: v1
kind: Service
metadata:
  name: build-back
  namespace: build
  labels:
    app: build-back
    app.kubernetes.io/part-of: build
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: build-back
    app.kubernetes.io/part-of: build
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
