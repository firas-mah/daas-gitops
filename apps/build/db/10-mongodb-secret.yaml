apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: build
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "admin"
  MONGO_INITDB_ROOT_PASSWORD: "build-mongodb-pass"
  MONGO_DB_NAME: "appdb"
