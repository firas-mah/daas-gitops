apiVersion: v1
kind: Service
metadata:
  name: manual-back
  namespace: manual
  labels:
    app: manual-back
    app.kubernetes.io/part-of: manual
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: manual-back
    app.kubernetes.io/part-of: manual
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
