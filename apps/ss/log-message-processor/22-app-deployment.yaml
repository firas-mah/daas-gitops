apiVersion: apps/v1
kind: Deployment
metadata:
  name: ss-log-message-processor
  namespace: ss
  labels:
    app: ss-log-message-processor
    app.kubernetes.io/part-of: ss
    app.kubernetes.io/component: log-message-processor
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ss-log-message-processor
      app.kubernetes.io/part-of: ss
      app.kubernetes.io/component: log-message-processor
  template:
    metadata:
      labels:
        app: ss-log-message-processor
        app.kubernetes.io/part-of: ss
        app.kubernetes.io/component: log-message-processor
    spec:
      containers:
        - name: log-message-processor
          image: docker.io/firasmah/ss-log-message-processor:run-1777533932020-log-message-processor
          ports:
            - containerPort: 8000
              protocol: TCP
          envFrom:
            - secretRef:
                name: ss-log-message-processor-secrets
                optional: true
            - configMapRef:
                name: ss-log-message-processor-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
