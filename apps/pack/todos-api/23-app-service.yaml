apiVersion: v1
kind: Service
metadata:
  name: pack-todos-api
  namespace: pack
  labels:
    app: pack-todos-api
    app.kubernetes.io/part-of: pack
    app.kubernetes.io/component: todos-api
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: pack-todos-api
    app.kubernetes.io/part-of: pack
    app.kubernetes.io/component: todos-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
