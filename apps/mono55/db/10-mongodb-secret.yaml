apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: mono55
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "admin"
  MONGO_INITDB_ROOT_PASSWORD: "mono55-mongodb-pass"
  MONGO_DB_NAME: "appdb"
