apiVersion: v1
kind: Service
metadata:
  name: mono55-back
  namespace: mono55
  labels:
    app: mono55-back
    app.kubernetes.io/part-of: mono55
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: mono55-back
    app.kubernetes.io/part-of: mono55
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
