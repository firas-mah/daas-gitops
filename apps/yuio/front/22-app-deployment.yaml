apiVersion: apps/v1
kind: Deployment
metadata:
  name: yuio-front
  namespace: yuio
  labels:
    app: yuio-front
    app.kubernetes.io/part-of: yuio
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: yuio-front
      app.kubernetes.io/part-of: yuio
      app.kubernetes.io/component: front
  template:
    metadata:
      labels:
        app: yuio-front
        app.kubernetes.io/part-of: yuio
        app.kubernetes.io/component: front
    spec:
      containers:
        - name: front
          image: docker.io/firasmah/yuio-front:run-1777874084764-front
          ports:
            - containerPort: 80
              protocol: TCP
          envFrom:
            - secretRef:
                name: yuio-front-secrets
                optional: true
            - configMapRef:
                name: yuio-front-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
