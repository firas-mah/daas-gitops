apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-back
  namespace: post
  labels:
    app: post-back
    app.kubernetes.io/part-of: post
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: post-back
      app.kubernetes.io/part-of: post
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app: post-back
        app.kubernetes.io/part-of: post
        app.kubernetes.io/component: back
    spec:
      containers:
        - name: back
          image: docker.io/firasmah/post-back:run-1776268691244-back
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: post-back-secrets
                optional: true
            - configMapRef:
                name: post-back-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
