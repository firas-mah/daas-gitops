apiVersion: v1
kind: Service
metadata:
  name: front
  namespace: daas
  labels:
    app: front
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: front
  ports:
    - name: http
      port: 8080
      targetPort: 80
      protocol: TCP
