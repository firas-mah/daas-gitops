apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-back
  namespace: front
  labels:
    app: front-back
    app.kubernetes.io/part-of: front
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front-back
      app.kubernetes.io/part-of: front
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app: front-back
        app.kubernetes.io/part-of: front
        app.kubernetes.io/component: back
    spec:
      containers:
        - name: back
          image: docker.io/firasmah/front-back:run-1775656178247-back
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: front-back-secrets
                optional: true
            - configMapRef:
                name: front-back-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
