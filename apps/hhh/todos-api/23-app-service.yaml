apiVersion: v1
kind: Service
metadata:
  name: hhh-todos-api
  namespace: hhh
  labels:
    app: hhh-todos-api
    app.kubernetes.io/part-of: hhh
    app.kubernetes.io/component: todos-api
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: hhh-todos-api
    app.kubernetes.io/part-of: hhh
    app.kubernetes.io/component: todos-api
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
