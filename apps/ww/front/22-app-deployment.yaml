apiVersion: apps/v1
kind: Deployment
metadata:
  name: ww-front
  namespace: ww
  labels:
    app: ww-front
    app.kubernetes.io/part-of: ww
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ww-front
      app.kubernetes.io/part-of: ww
      app.kubernetes.io/component: front
  template:
    metadata:
      labels:
        app: ww-front
        app.kubernetes.io/part-of: ww
        app.kubernetes.io/component: front
    spec:
      containers:
        - name: front
          image: docker.io/firasmah/ww-front:run-1775919325048-front
          ports:
            - containerPort: 80
              protocol: TCP
          envFrom:
            - secretRef:
                name: ww-front-secrets
                optional: true
            - configMapRef:
                name: ww-front-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
