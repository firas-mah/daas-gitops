apiVersion: apps/v1
kind: Deployment
metadata:
  name: ww-back
  namespace: ww
  labels:
    app: ww-back
    app.kubernetes.io/part-of: ww
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ww-back
      app.kubernetes.io/part-of: ww
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app: ww-back
        app.kubernetes.io/part-of: ww
        app.kubernetes.io/component: back
    spec:
      containers:
        - name: back
          image: docker.io/firasmah/ww-back:run-1775919324824-back
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: ww-back-secrets
                optional: true
            - configMapRef:
                name: ww-back-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
