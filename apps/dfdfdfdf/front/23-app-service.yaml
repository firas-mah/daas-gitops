apiVersion: v1
kind: Service
metadata:
  name: dfdfdfdf-front
  namespace: dfdfdfdf
  labels:
    app: dfdfdfdf-front
    app.kubernetes.io/part-of: dfdfdfdf
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: dfdfdfdf-front
    app.kubernetes.io/part-of: dfdfdfdf
    app.kubernetes.io/component: front
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
