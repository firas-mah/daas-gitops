apiVersion: apps/v1
kind: Deployment
metadata:
  name: dfdfdfdf-back
  namespace: dfdfdfdf
  labels:
    app: dfdfdfdf-back
    app.kubernetes.io/part-of: dfdfdfdf
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dfdfdfdf-back
      app.kubernetes.io/part-of: dfdfdfdf
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app: dfdfdfdf-back
        app.kubernetes.io/part-of: dfdfdfdf
        app.kubernetes.io/component: back
    spec:
      containers:
        - name: back
          image: docker.io/firasmah/dfdfdfdf-back:run-1777615843764-back
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: dfdfdfdf-back-secrets
                optional: true
            - configMapRef:
                name: dfdfdfdf-back-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
