apiVersion: v1
kind: Service
metadata:
  name: testapp-back
  namespace: testapp
  labels:
    app: testapp-back
    app.kubernetes.io/part-of: testapp
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: testapp-back
    app.kubernetes.io/part-of: testapp
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
