apiVersion: apps/v1
kind: Deployment
metadata:
  name: gggrrr-front
  namespace: gggrrr
  labels:
    app: gggrrr-front
    app.kubernetes.io/part-of: gggrrr
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gggrrr-front
      app.kubernetes.io/part-of: gggrrr
      app.kubernetes.io/component: front
  template:
    metadata:
      labels:
        app: gggrrr-front
        app.kubernetes.io/part-of: gggrrr
        app.kubernetes.io/component: front
    spec:
      containers:
        - name: front
          image: docker.io/firasmah/gggrrr-front:run-1777868540969-front
          ports:
            - containerPort: 80
              protocol: TCP
          envFrom:
            - secretRef:
                name: gggrrr-front-secrets
                optional: true
            - configMapRef:
                name: gggrrr-front-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
