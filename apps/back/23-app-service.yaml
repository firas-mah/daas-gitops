apiVersion: v1
kind: Service
metadata:
  name: back
  namespace: daas
  labels:
    app: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
