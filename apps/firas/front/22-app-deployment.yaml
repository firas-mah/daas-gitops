apiVersion: apps/v1
kind: Deployment
metadata:
  name: firas-front
  namespace: firas
  labels:
    app: firas-front
    app.kubernetes.io/part-of: firas
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: firas-front
      app.kubernetes.io/part-of: firas
      app.kubernetes.io/component: front
  template:
    metadata:
      labels:
        app: firas-front
        app.kubernetes.io/part-of: firas
        app.kubernetes.io/component: front
    spec:
      containers:
        - name: front
          image: docker.io/firasmah/firas-front:run-1775907968434-front
          ports:
            - containerPort: 80
              protocol: TCP
          envFrom:
            - secretRef:
                name: firas-front-secrets
                optional: true
            - configMapRef:
                name: firas-front-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
