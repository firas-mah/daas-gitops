apiVersion: v1
kind: Service
metadata:
  name: monotest3-back
  namespace: monotest3
  labels:
    app: monotest3-back
    app.kubernetes.io/part-of: monotest3
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: monotest3-back
    app.kubernetes.io/part-of: monotest3
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
