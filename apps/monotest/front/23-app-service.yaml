apiVersion: v1
kind: Service
metadata:
  name: monotest-front
  namespace: monotest
  labels:
    app: monotest-front
    app.kubernetes.io/part-of: monotest
    app.kubernetes.io/component: front
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: monotest-front
    app.kubernetes.io/part-of: monotest
    app.kubernetes.io/component: front
  ports:
    - name: http
      port: 8080
      targetPort: 80
      protocol: TCP
