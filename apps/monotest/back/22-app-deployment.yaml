apiVersion: apps/v1
kind: Deployment
metadata:
  name: monotest-back
  namespace: monotest
  labels:
    app: monotest-back
    app.kubernetes.io/part-of: monotest
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monotest-back
      app.kubernetes.io/part-of: monotest
      app.kubernetes.io/component: back
  template:
    metadata:
      labels:
        app: monotest-back
        app.kubernetes.io/part-of: monotest
        app.kubernetes.io/component: back
    spec:
      containers:
        - name: back
          image: docker.io/firasmah/monotest-back:run-1774544287829-back
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: monotest-back-secrets
                optional: true
            - configMapRef:
                name: monotest-back-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
