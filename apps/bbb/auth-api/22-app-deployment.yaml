apiVersion: apps/v1
kind: Deployment
metadata:
  name: bbb-auth-api
  namespace: bbb
  labels:
    app: bbb-auth-api
    app.kubernetes.io/part-of: bbb
    app.kubernetes.io/component: auth-api
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bbb-auth-api
      app.kubernetes.io/part-of: bbb
      app.kubernetes.io/component: auth-api
  template:
    metadata:
      labels:
        app: bbb-auth-api
        app.kubernetes.io/part-of: bbb
        app.kubernetes.io/component: auth-api
    spec:
      containers:
        - name: auth-api
          image: docker.io/firasmah/bbb-auth-api:run-1776789424851-auth-api
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: bbb-auth-api-secrets
                optional: true
            - configMapRef:
                name: bbb-auth-api-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
