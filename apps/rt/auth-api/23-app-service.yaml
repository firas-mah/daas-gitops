apiVersion: v1
kind: Service
metadata:
  name: rt-auth-api
  namespace: rt
  labels:
    app: rt-auth-api
    app.kubernetes.io/part-of: rt
    app.kubernetes.io/component: auth-api
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: rt-auth-api
    app.kubernetes.io/part-of: rt
    app.kubernetes.io/component: auth-api
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
