apiVersion: apps/v1
kind: Deployment
metadata:
  name: rt-auth-api
  namespace: rt
  labels:
    app: rt-auth-api
    app.kubernetes.io/part-of: rt
    app.kubernetes.io/component: auth-api
    managed-by: daas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rt-auth-api
      app.kubernetes.io/part-of: rt
      app.kubernetes.io/component: auth-api
  template:
    metadata:
      labels:
        app: rt-auth-api
        app.kubernetes.io/part-of: rt
        app.kubernetes.io/component: auth-api
    spec:
      containers:
        - name: auth-api
          image: docker.io/firasmah/rt-auth-api:run-1777619851436-auth-api
          ports:
            - containerPort: 8081
              protocol: TCP
          envFrom:
            - secretRef:
                name: rt-auth-api-secrets
                optional: true
            - configMapRef:
                name: rt-auth-api-config
                optional: true
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
          readinessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: 8081
            initialDelaySeconds: 90
            periodSeconds: 10
            failureThreshold: 3
