apiVersion: v1
kind: Service
metadata:
  name: fdfdfdfd-back
  namespace: fdfdfdfd
  labels:
    app: fdfdfdfd-back
    app.kubernetes.io/part-of: fdfdfdfd
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: fdfdfdfd-back
    app.kubernetes.io/part-of: fdfdfdfd
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
