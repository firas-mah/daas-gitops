apiVersion: v1
kind: Service
metadata:
  name: zeee-back
  namespace: zeee
  labels:
    app: zeee-back
    app.kubernetes.io/part-of: zeee
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: zeee-back
    app.kubernetes.io/part-of: zeee
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8080
      targetPort: 8081
      protocol: TCP
