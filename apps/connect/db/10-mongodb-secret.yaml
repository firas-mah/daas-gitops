apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: connect
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "admin"
  MONGO_INITDB_ROOT_PASSWORD: "connect-mongodb-pass"
  MONGO_DB_NAME: "appdb"
