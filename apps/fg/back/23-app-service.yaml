apiVersion: v1
kind: Service
metadata:
  name: fg-back
  namespace: fg
  labels:
    app: fg-back
    app.kubernetes.io/part-of: fg
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: fg-back
    app.kubernetes.io/part-of: fg
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
