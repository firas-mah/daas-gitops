apiVersion: v1
kind: Service
metadata:
  name: df-back
  namespace: df
  labels:
    app: df-back
    app.kubernetes.io/part-of: df
    app.kubernetes.io/component: back
    managed-by: daas
spec:
  type: ClusterIP
  selector:
    app: df-back
    app.kubernetes.io/part-of: df
    app.kubernetes.io/component: back
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
